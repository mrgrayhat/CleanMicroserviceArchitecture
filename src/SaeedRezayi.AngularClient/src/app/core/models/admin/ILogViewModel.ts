export interface ILogViewModel {
    id: string,
    timestamp: string;
    level: string;
    exception: string;
    messageTemplate: string;
    renderedMessage: string;
    utcTimestamp: string;
    
}

